<% if @child.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
    The form contains<%= pluralize(@child.errors.count, "error") %>.
    </div>
    <ul>
      <% @child.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
