<% if @growth.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
    The form contains<%= pluralize(@growth.errors.count, "error") %>.
    </div>
    <ul>
      <% @growth.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
