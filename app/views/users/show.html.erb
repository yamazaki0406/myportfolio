<%= provide(:title, @user.name)%>
<div class="row">
<aside class="col-md-4">
<section class="user_info">
<h1><%= @user.name %>さんのページです</h1>
</section>
</aside>
</div>

<%= @child.name %><%= link_to 'Show', child_path(@child) %>
<%= link_to 'Edit', edit_child_path(@child) %>
<%= link_to 'Destroy', child_path(@child), method: :delete, data: { confirm: 'Are you sure?' } %>
</tbody>
